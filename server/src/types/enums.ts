export enum UserRole {
    Basic = 'basic',
    Premium = 'premium',
}

export enum SystemError {
    NotFound = 'NotFound',
    EnvError = 'EnvError',
    Forbidden = 'Forbidden',
    Unauthorized = 'Unauthorized',
}
